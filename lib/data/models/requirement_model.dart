class RequirementModel {
  final String? tilte, budget, createdBy, category, status;

  RequirementModel({
     this.tilte,
     this.createdBy,
     this.budget,
     this.category,
     this.status
  });
}