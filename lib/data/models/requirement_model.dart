class RequirementModel {
  final String? title, budget, createdBy, category, status;

  RequirementModel({
     this.title,
     this.createdBy,
     this.budget,
     this.category,
     this.status
  });
}