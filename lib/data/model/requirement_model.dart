class RequirementModel {
  final String? title, budget, createdBy, category, status, pieces, material, surface, size, createdDate, decription;

  RequirementModel({
     this.title,
     this.createdBy,
     this.budget,
     this.category,
     this.status,
     this.pieces,
     this.createdDate,
     this.material,
     this.size,
     this.surface,
     this.decription
  });
}